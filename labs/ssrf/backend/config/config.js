/**
 * @file config/config.js
 * @description a global configuration file for all the apis config
 */

/**
 * the port the server will run on (8080)
 * @constant {number}
 * @default 8080
*/
const PORT = 8080
/**
 * config module
 * @module config
 * @property {number} PORT - the http server port
 */

module.exports = { PORT }
